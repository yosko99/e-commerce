import styled from 'styled-components';

const OnSaleLabel = styled.div`
  position: absolute;
  top: 1rem;
  left: 0.5rem;
  z-index: 100;
  padding: 0.2rem 0.5rem 0.2rem 0.5rem;
  font-size: 0.8rem;
  background-color: #b80f0a;
  font-weight: 700;
  color: white;
`;

export default OnSaleLabel;
